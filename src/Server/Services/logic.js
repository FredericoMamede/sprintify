const { createNewSprint } = require("../Data/Crud");

// Manager!!

// Create new Sprint
export const newSprint = { project, startDate, endDate };

// Create new Task
export const task = { description, department, project, completed };


